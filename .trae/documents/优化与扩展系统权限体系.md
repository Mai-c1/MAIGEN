## 全量权限体系重构与增补方案

### 1. SQL 脚本全量更新
在 `init.sql` 中插入涵盖以下所有维度的 50+ 个细分权限：
- **任务管理**: 涵盖从创建、执行、日志查看、到数据导出的全生命周期。
- **AI 治理**: 包含模型配置、Prompt 策略、Token 消耗统计及配额管理。
- **社区生态**: 增补审核、评论管理、举报处理、标签管理等互动权限。
- **财务审计**: 细化积分配额、变动流水、规则配置等权限。
- **系统运维**: 增加登录日志、操作审计、队列监控、全员通知发布等高级权限。

### 2. 命名规范与分类优化
- 统一采用 `object:sub-object:action` 的三段式命名。
- 将权限按业务逻辑重新分组，便于前端权限树展示。

### 3. 后端权限码同步
- 搜索并替换后端代码中现有的权限字符串，确保鉴权逻辑与新 SQL 一致。
- 自动为 `admin` 角色关联所有新增权限。

**确认后我将直接输出完整的 SQL 脚本并应用到 `docs/init.sql`。**