## 任务创建页重构方案：上下分层流 (Top-Down Flow)

根据您的选择，我将把布局调整为更符合自然阅读顺序的上下结构，解决之前左右分栏导致的失衡感。

### 1. 布局结构重构 (Structure)
- **第一行：核心定义 (Row 1)**
    - **题目名称 (Span 10)**：使用独立卡片，强化标题输入。
    - **限制与规模 (Span 14)**：将时间、内存限制与测试用例数量组合在一起，形成“规格配置”卡片。
- **第二行：策略中心 (Row 2)**
    - **生成策略选择 (Full Span 24)**：使用大卡片承载，策略网格调整为 **3列或4列布局**，充分利用横向空间，使策略描述展示更完整。
- **辅助增强 (Auxiliary)**
    - **填写建议**：不再使用独立的大卡片，而是转化为策略下方的“小贴士”条（Tips Bar）或集成在输入项的 `extra` 描述中，保持页面清爽。

### 2. 交互与视觉优化 (Visuals)
- **参数对齐**：通过栅格系统确保顶部两组卡片高度一致。
- **策略卡片升级**：增加更明显的选中动效（如光晕或缩放），提升“点击感”。
- **步骤条优化**：微调步骤条间距，使其与下方分层布局视觉中心一致。

### 3. 修改文件
- **[create.vue](file:///d:/web/MAIGEN2/frontend/src/views/task/create.vue)**：彻底重构 `<template>` 中的第一步代码块。

请确认此方案。
