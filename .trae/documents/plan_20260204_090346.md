## 优化登录时效与主题切换体验

### 1. 后端：延长登录有效期
- **修改文件**: `backend/MAIGEN-api/src/main/resources/application.yml`
- **操作**: 显式配置 `sa-token.timeout` 为 `2592000` (30天)，确保用户无需频繁登录。

### 2. 前端：主题持久化与初始化修复
- **Store 优化**: 检查 `src/store/app.ts`，新增 `toggleTheme` 方法以便于一键切换。
- **全局初始化**: 在 `src/App.vue` 或 `src/main.ts` 中尽早调用 `initTheme()`，解决刷新页面主题闪烁或重置的问题。

### 3. 前端：UI 交互升级 (去下拉框)
- **通用组件**: 封装一个 `ThemeToggle` 按钮组件（可选，或直接在各页面实现），点击直接在 `dark/light` 间切换。
- **主布局 (MainLayout)**: 移除现有的 Dropdown，替换为直观的 🌞/🌙 切换按钮。
- **管理后台 (AdminLayout)**: 在顶部导航栏右侧添加同款切换按钮。
- **登录页 (Login)**: 在右上角悬浮添加主题切换按钮，提升视觉体验。

**请确认是否执行此方案？**