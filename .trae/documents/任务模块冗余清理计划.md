## 任务模块冗余清理计划

经过对创建任务及结果处理流程的深入分析，结合您的逻辑说明，确认 `task_testcase` 表及相关代码确实属于冗余。以下是详细分析与清理计划：

### **1. 流程分析与表冗余确认**
- **`task_testcase` (冗余)**: 您提到的逻辑非常明确，测试点的具体构造参数和策略仅在 `analysis` 模块中使用，不进行持久化。当前 `MAIGEN-api` 流程中也确实没有往该表写入数据的逻辑。
- **`task_execution_log` (冗余)**: 目前 AI 流水线的处理日志未在数据库中记录。如果您倾向于不持久化这些中间过程，该表同样是多余的。
- **`task_strategy` (保留)**: 该表作为“策略字典”，存储了系统支持的策略名称（如“基础随机”等），供用户在创建任务时选择及回显策略名称，不可删除。
- **`task` & `generated_data` (保留)**: 存储任务基本配置和最终生成的 ZIP 下载链接，是核心业务表。

### **2. 清理步骤**

#### **后端代码清理**
1.  **删除冗余 VO**: 移除 `TaskTestcaseVO` 和 `TaskLogVO`。
2.  **删除冗余实体与接口**: 
    - 物理删除 `TaskTestcase.java`、`TaskExecutionLog.java` 及其对应的 Mapper、Service 和 XML。
3.  **重构 TaskController**: 
    - 移除 `GET /task/testcases/{taskId}` 和 `GET /task/logs/{taskId}` 接口。
4.  **重构 TaskServiceImpl**:
    - 移除对 `TaskTestcaseService` 和 `TaskExecutionLogService` 的依赖。
    - 清理 `getTaskDetail`、`deleteTask` 等方法中涉及这两张表的操作逻辑。
    - 在 `getTaskDetail` 中，仅从 Redis 或 `task` 表获取必要信息（如需永久保留用户选择的策略，建议在 `task` 表增加一个 JSON 字段，而非使用关联表）。

#### **文档与数据库清理**
1.  **更新 SQL 脚本**: 在 `init.sql` 中移除这两张表的定义。
2.  **更新设计文档**: 同步更新 `数据库设计方案.md`，确保文档与代码一致。

您是否同意移除这两张表及相关的查询接口？确认后我将执行清理工作。