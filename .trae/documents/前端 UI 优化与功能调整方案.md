## 1. 任务详情页面 (`detail.vue`) 深度重构
- **新增“任务执行终端” (Execution Console)**：
    - **视觉设计**：在右侧区域引入一个类控制台的黑色卡片，使用 `JetBrains Mono` 等宽字体，模拟终端输出。
    - **动态日志流**：根据后端返回的 `status` 实时生成带有时间戳的执行日志。
        - *例如：`[10:24:05] [AGENT] 正在进行题目语义分析...`*
    - **沉浸式体验**：日志将像聊天信息一样逐行呈现，并自动滚动至最新位置，清晰展示任务进度的每一个阶段。
- **独立“错误诊断”模块**：
    - 当任务状态为失败时，在终端或页面顶部弹出专门的红色告警区域，清晰展示 `errorMessage`，并配有“一键重试”按钮。
- **题目描述滚动条**：
    - 为“题目需求”卡片设置 `max-height: 400px` 的限制，并添加美化的滚动条，解决页面因内容过长而难以浏览的问题。
- **布局重组**：
    - 采用 **左 14/24 (内容区)** + **右 10/24 (监控区)** 的黄金比例布局。
    - 左侧展示题目与代码，右侧展示执行终端与任务配置，信息层级更加鲜明。
- **清理冗余信息**：
    - 移除顶部状态栏中重复的任务 ID 和状态文字，将核心状态整合进终端与右侧面板。

## 2. 主题色体系扩充
- **新增多款彩色主题**：除了经典的极光蓝，新增 `Midnight (深海紫)`、`Sakura (樱花粉)`、`Cyan (极客青)` 等色彩方案。
- **全链路更新**：同步修改 `MainLayout.vue` 的切换器、`appStore` 的状态管理以及 `style.css` 中的全局 CSS 变量。

## 3. 管理后台入口优化
- **入口迁移**：将“管理后台”从左侧菜单栏移除，整合进右上角头像的下拉菜单中，保持侧边栏的专注性。
- **权限校验**：修正 `isAdmin` 逻辑，确保该入口仅对 `role === 'admin'` 的用户可见，增强安全性。

请审阅以上修订后的方案，确认无误后我将立即开始实施。